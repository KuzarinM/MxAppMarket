
# 📦 MxStore

**MxStore** — это легковесный, локальный Self-Hosted каталог приложений (AppStore).

Решение предназначено для организации локальной библиотеки инсталляторов (`.exe`, `.msi`, `.zip`) с красивым веб-интерфейсом, автоматическим сбором метаданных, поддержкой версионирования и удобным поиском. Идеально подходит для системных администраторов, DevOps-инженеров или для домашней коллекции софта.


## ✨ Основные возможности

*   **🕵️ Умное сканирование**: Автоматически сканирует указанные папки, находит инсталляторы и распределяет их по структуре `Программа -> Версия`.
*   **🧠 Авто-обогащение данных**: Подтягивает иконки, описания и скриншоты из открытых источников (Flathub, iTunes API) на основе названия файла.
*   **🎨 Современный UI**: Адаптивный интерфейс на **Bootstrap 5** с поддержкой **Тёмной/Светлой темы**.
*   **🏷️ Система тегов и поиск**: Мгновенная фильтрация по тегам и текстовый поиск.
*   **📝 Markdown описания**: Рендеринг красивых описаний программ с поддержкой списков, ссылок и форматирования.
*   **👥 Управление доступом**: Встроенная система пользователей и ролей (Admin/User).
*   **🐳 Docker-Ready**: Полная поддержка запуска в контейнерах с монтированием внешних томов.

## 🛠 Технический стек

Проект построен на современном стеке технологий .NET:

*   **Core**: .NET 8 (Razor Pages)
*   **Database**: SQLite + Entity Framework Core
*   **UI**: Bootstrap 5.3 + Bootstrap Icons
*   **Markdown**: Markdig
*   **Background Tasks**: `IHostedService` для фонового сканирования и дедупликации.

## 🚀 Запуск через Docker (Рекомендуется)

Это самый простой способ запустить MxStore. Вам понадобится папка с инсталляторами и папка для базы данных/картинок.

### Структура папок хоста
*   `/path/to/installers` — ваша коллекция софта (можно Read-Only).
*   `/path/to/data` — здесь будет создана БД (`packages.db`) и скачаны иконки (Read-Write).

### Команда запуска

```bash
docker run -d \
  --name mxstore \
  -p 8080:80 \
  -v /path/to/installers:/installers \
  -v /path/to/data:/data \
  -e APP_DATA_PATH="/data" \
  -e AppConfig__DefaultScanPath="/installers" \
  your-docker-hub/mxstore:latest
```

### Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|All |---|---|
| `APP_DATA_PATH` | Путь, где хранится БД и картинки | `/app/App_Data` |
| `AppConfig__DefaultScanPath` | Путь к папке с инсталляторами для сканирования | `C:\Installers` (win) |

## 🏁 Начало работы

1.  Запустите приложение.
2.  Перейдите по адресу `http://localhost:8080`.
3.  **Вход по умолчанию:**
    *   Логин: `admin`
    *   Пароль: `admin`
4.  Перейдите в раздел **Администрирование** -> **Сканировать папку**.
5.  Дождитесь окончания сканирования. Ваши программы появятся в каталоге!

> ⚠️ **Важно:** После первого входа настоятельно рекомендуется сменить пароль администратора в личном кабинете.

## 🤝 Соавторство и Разработка

Этот проект является результатом плотного сотрудничества человека и искусственного интеллекта.

*   **Автор идеи и Архитектор**: [Ваш Никнейм/Имя]
*   **Code Assistant & Co-Author**: **AI (LLM)** — *активный участник проекта*.

ИИ принимал участие в написании более 80% кодовой базы, включая:
*   Разработку архитектуры сервисов (Scanner, Metadata Aggregator).
*   Реализацию Frontend-логики (Razor Pages, JS-фильтры).
*   Оптимизацию Docker-сценариев и EF Core запросов.
*   Рефакторинг и устранение уязвимостей.

*Created with ❤️ and 🤖 logic.*

